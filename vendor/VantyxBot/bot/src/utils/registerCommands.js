module.exports = async (client) => {
  // --- AIO Commands are registered automatically by `src/handlers/loaders/commands.js` ---
  // VantyxBot no longer needs to push REST updates for them manually.
  return;
};
